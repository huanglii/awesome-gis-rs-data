import { name, homepage } from '../../package.json'

const githubUrl = `https://github.com/huanglii/${name}`
const dataUrl = `https://github.com/huanglii/${name}/blob/master/config/data.json`

export default {
  packageName: name,
  homepage,
  githubUrl,
  dataUrl
}
